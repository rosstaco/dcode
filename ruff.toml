line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = [
    "E501",  # line length handled by formatter; allow long strings
]

[lint.per-file-ignores]
"tests/*" = ["E501"]
